-- typescript-language-server: javascript / typescript (+ react variants).
-- cmd, filetypes and root_markers come from nvim-lspconfig defaults; ts_ls
-- also supports single-file mode, so it attaches to standalone .js files.
return {}
